import { NavRootDirective } from './nav-root.directive';

describe('NavRootDirective', () => {
  it('should create an instance', () => {
    const directive = new NavRootDirective();
    expect(directive).toBeTruthy();
  });
});
