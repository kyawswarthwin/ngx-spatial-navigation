import { NavSectionDirective } from './nav-section.directive';

describe('NavSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new NavSectionDirective();
    expect(directive).toBeTruthy();
  });
});
