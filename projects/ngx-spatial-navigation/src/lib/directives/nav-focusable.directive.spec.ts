import { NavFocusableDirective } from './nav-focusable.directive';

describe('NavFocusableDirective', () => {
  it('should create an instance', () => {
    const directive = new NavFocusableDirective();
    expect(directive).toBeTruthy();
  });
});
